apiVersion: v1
kind: Service
metadata:
  name: eveil-service
spec:
  type: NodePort
  selector:
    app: eveil
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
